<div class="offer-carousel-item">
    <a href="{{ $url }}" class="offer-carousel-item__thumb">
        <img src="{{ $thumb }}" alt="{{ $title }}">
    </a>
    <div class="offer-carousel-item__content">
        <h4 class="offer-carousel-item__title">
            {{ $title }}
        </h4>
        <a href="{{ $url }}" class="offer-carousel-item__btn"></a>
    </div>
</div>
